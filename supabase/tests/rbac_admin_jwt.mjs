/**
 * Legacy live test retired for V1 hardening.
 * Recreate locally only with explicit env vars and disposable test data.
 */
console.error('supabase/tests/rbac_admin_jwt.mjs has been retired. Rebuild it with explicit env configuration before use.');
process.exit(1);
